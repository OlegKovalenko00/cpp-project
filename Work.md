# 🚀 Инструкция по совместной работе над проектом MetricSys

Эта инструкция описывает, **как мы работаем командой**, чтобы соблюдать требования преподавателя и нормальные индустриальные практики.

---

# 1. Общие правила работы

1. **Каждый сервис — отдельный репозиторий**:

   * `metrics-service`
   * `aggregation-service`
   * `api-service`
   * `monitoring-service`

2. **Главный репозиторий-оркестратор** содержит:

   * `docker-compose.yml`
   * общие инструкции
   * документацию
   * ссылку на каждый сервис

3. В каждом репозитории работают **только ответственные за сервис**,
   но остальные могут оставлять review и помогать.

4. Нельзя пушить в `main` напрямую — только через **Pull Requests** (PR).

---

# 2. Ветки (branching)
* `main` — стабильная ветка, всегда рабочий код
* Каждая задача → отдельная ветка:
    пишете новую функцию(группу функций)->создали ветку под это 

```
feature/<name>
bugfix/<name>
refactor/<name>
docs/<name>
```

Примеры:

```
feature/add-aggregation-logic
bugfix/db-connection-timeout
docs/update-readme
```

Создаём ветку:

```bash
git checkout -b feature/my-task
```

---

# 3. Issues (задачи)

Каждая задача фиксируется в **GitHub Issues**.

В Issue обязательно пишем:

* Что нужно сделать
* Acceptance Criteria (что означает «готово»)
* Labels: `backend`, `database`, `priority: high`, …

Назначаем ответственного → **Assignee**.

Пример:

```
Issue #12: Реализовать эндпоинт GET /metrics/latest
```

---

# 4. Работа через Pull Request (PR)

Когда задача сделана:

1. Пушим ветку:

   ```bash
   git push origin feature/my-task
   ```

2. Создаём PR → `feature/my-task` → `main`.

3. В PR пишем:

   * Краткое описание
   * Ссылку на Issue:

     ```
     Closes #12
     ```

4. Требования к PR:

   * ❗ Минимум 1 approve от другого участника
   * ❗ CI должен пройти (GitHub Actions)

5. Автор PR сам НЕ мерджит — это делает команда (или лид).

---

# 5. Commit-правила

Большие задачи разбиваем на маленькие коммиты.

Формат сообщений:

```
[Category] Короткое описание
```

Категории:

* `feat`: новая функция
* `fix`: исправление
* `refactor`: рефакторинг
* `test`: тесты
* `docs`: документация
* `infra`: инфраструктура (Docker, CI)

Пример:

```
feat: добавил модель AggregatedMetric
fix: исправил падение при пустой выборке из БД
docs: обновил инструкции по запуску
```


# 6. Коротко — рабочий цикл каждого участника

1. Получить задачу из Issues
2. Создать ветку
3. Написать код
4. Сделать PR
5. Получить review → исправить замечания
6. Merge в main

---
